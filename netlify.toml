[build]
  command = "npm run build:prod"
  publish = "src/_site"

[build.environment]
  NODE_VERSION = "21"
  NPM_VERSION = "10"
  RUBY_VERSION = "3.2.2"
  JEKYLL_ENV = "production"
  NODE_ENV = "production"
